<!DOCTYPE html>
<html lang="en">
<head>
    <title>Favorite</title>
    <meta charset="UTF-8">
    <link rel="shortcut icon" href="weather_few_clouds.ico" type="image/x-icon">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,700,0,200" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="saved.css">
</head>
<body>
<div id="left-nav"></div>
<div class="container">
    <div class="data">
            <% favLoc.forEach(function(a){ %>  
            <div class="eachcity">
                <div>
                    <a href="/<%= a.city %>"><%= a.city %></a>
                    <form action="/delete" method="post">
                        <input type="hidden" name="checkbox" value=<%= a._id  %>>
                        <button style="border-radius: 5px; padding-top: 6px; cursor: pointer; background-color:  #e7f1f6; border: none;" type="submit" class="delete"><span style="cursor: pointer; font-size: 18px;" class="dots material-symbols-outlined">delete</span></button>
                    </form>
                </div>
                <p><img style="width: 40%; background-color: #a6b0b7; border-radius: 50%;" src=<%= a.icon %>  alt=""><%- a.temp %>°C</p>
            </div>
            <% }) %>
            <div class="addcity">
                <form action="/fav" method="post">
                    <input type="text" name="cityname" placeholder="Add city...">
                    <button type="submit"><span class="material-symbols-outlined">add</span></button>
                </form>
            </div>  
    </div>
    <% if(message.length > 0){ %>
        <div class="alert alert-success" style="width: 20%; margin-left: auto; margin-right: auto; text-align: center;">
            <%= message %> 
        </div>
    <% } %> 
</div>
<div id="bottomNav">
    <div class="bottomBar">
        <a href="/city"><span id="hhh" class="material-symbols-outlined">home</span></a>
        <a href="/fav"><span id="sss" class="material-symbols-outlined">favorite</span></a>
        <a href="/map"><span id="mmm" class="material-symbols-outlined">map</span></a>
        <a href="/user/logout"><span id="lll" class="material-symbols-outlined">logout</span></a>
    </div>
</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="saved.js"></script>
</body>
</html> 